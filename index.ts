


// Inferring types =  The TypeScript compiler can infer the type of x based on the expressions that are used to initialize it

console.log("Hello Erum Sikander");
let obj = {
name: 'Erum Sikander',
age : 24,
city : 'Hyderabad', 
developer : true,
}
console.log(obj);
// uses ---- JSon Stand for Javascript Object Notation and json is a text-based data format that is used to store and transfer data:

console.log(JSON.stringify(obj));
